<Window x:Class="FileCounterPro_Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FileCounter Pro (Windows Edition)" Height="750" Width="1100"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Border CornerRadius="12" Background="{StaticResource SciFiBgDeep}" BorderBrush="{StaticResource SciFiNeonCyan}" BorderThickness="1">
        <Grid>
            <!-- Title Bar for dragging -->
            <Border Height="35" VerticalAlignment="Top" Background="#05050C" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" CornerRadius="12,12,0,0">
                <Grid>
                    <TextBlock Text="FILE COUNTER PRO" Foreground="{StaticResource SciFiTextDim}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="15,0,0,0" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,0">
                        <Button Content="—" Width="30" Height="25" Background="Transparent" Foreground="White" BorderThickness="0" Click="BtnMinimize_Click"/>
                        <Button Content="✕" Width="30" Height="25" Background="Transparent" Foreground="#FF3333" BorderThickness="0" Click="BtnClose_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Margin="0,35,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="260"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <Border Grid.Column="0" Background="{StaticResource SciFiBgPanel}" BorderBrush="{StaticResource SciFiNeonCyan}" BorderThickness="0,0,1,0">
                    <StackPanel Margin="15,30,15,0">
                        <TextBlock Text="MODULES" FontSize="12" Foreground="{StaticResource SciFiTextDim}" Margin="5,0,0,10" FontWeight="Bold"/>
                        
                        <Button Content="📊 Dashboard" Style="{StaticResource NeonButtonStyle}" Click="NavDashboard_Click"/>
                        <Button Content="🛡️ Virus Scanner" Style="{StaticResource NeonButtonStyle}" Click="NavVirusScanner_Click"/>
                        <Button Content="🗑️ Smart Uninstaller" Style="{StaticResource NeonButtonStyle}" Click="NavUninstaller_Click"/>
                        <Button Content="💻 Hardware Analyzer" Style="{StaticResource NeonButtonStyle}" Click="NavHardware_Click"/>
                        <Button Content="⚡ Live Power" Style="{StaticResource NeonButtonStyle}" Click="NavPower_Click"/>
                        <Button Content="🌐 Network Monitor" Style="{StaticResource NeonButtonStyle}" Click="NavNetwork_Click"/>
                        <Button Content="🧹 System Junk" Style="{StaticResource NeonButtonStyle}" Click="NavJunk_Click"/>
                        <Button Content="👯 Duplicate Finder" Style="{StaticResource NeonButtonStyle}" Click="NavDuplicate_Click"/>
                    </StackPanel>
                </Border>

                <!-- Main Content Area -->
                <ContentControl x:Name="MainContent" Grid.Column="1" Margin="20">
                    <ContentControl.Style>
                        <Style TargetType="ContentControl">
                            <!-- Fade animation for views -->
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="ContentControl.TargetUpdated">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.0" To="1.0" Duration="0:0:0.3"/>
                                            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="20,40,20,0" To="20,20,20,20" Duration="0:0:0.3">
                                                <ThicknessAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </ThicknessAnimation.EasingFunction>
                                            </ThicknessAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentControl.Style>
                </ContentControl>
            </Grid>
        </Grid>
    </Border>
</Window>
