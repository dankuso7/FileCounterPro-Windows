<UserControl x:Class="FileCounterPro_Windows.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="FILE &amp; FOLDER DASHBOARD" FontSize="32" FontWeight="Black" Foreground="{StaticResource SciFiTextPrimary}" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="Scan deep folder hierarchies or create ZIP archives instantly." FontSize="14" Foreground="{StaticResource SciFiTextDim}" HorizontalAlignment="Center" Margin="0,0,0,40"/>

            <Border Style="{StaticResource SciFiCardStyle}" Width="400" Height="200">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock x:Name="TxtStatus" Text="READY TO SCAN" Foreground="{StaticResource SciFiNeonCyan}" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock x:Name="TxtCount" Text="0" Foreground="{StaticResource SciFiNeonCyan}" FontSize="64" FontWeight="Black" HorizontalAlignment="Center"/>
                    <TextBlock Text="Total Files Discovered" Foreground="{StaticResource SciFiTextDim}" FontSize="12" HorizontalAlignment="Center" Margin="0,-10,0,0"/>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,40,0,0">
                <Button Content="📁 Select Folder to Count" Style="{StaticResource NeonButtonStyle}" Width="250" Click="BtnCount_Click" Margin="10"/>
                <Button Content="📦 Select Folder to ZIP" Style="{StaticResource NeonButtonStyle}" Width="250" Click="BtnZip_Click" Margin="10"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
