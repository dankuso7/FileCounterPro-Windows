<UserControl x:Class="FileCounterPro_Windows.Views.HardwareAnalyzerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,0">
            <TextBlock Text="HARDWARE DIAGNOSTICS" FontSize="32" FontWeight="Black" Foreground="{StaticResource SciFiTextPrimary}" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="Live WMI polling for critical system infrastructure." FontSize="14" Foreground="{StaticResource SciFiTextDim}" HorizontalAlignment="Center" Margin="0,0,0,40"/>

            <WrapPanel HorizontalAlignment="Center" MaxWidth="800">
                <!-- CPU Info -->
                <Border Style="{StaticResource SciFiCardStyle}" Width="350" Height="150" Margin="10">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="CPU ARCHITECTURE" Foreground="{StaticResource SciFiNeonCyan}" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBlock x:Name="TxtCPUName" Text="Querying WMI..." Foreground="{StaticResource SciFiTextPrimary}" FontSize="18" TextWrapping="Wrap"/>
                        <TextBlock x:Name="TxtCPUCores" Text="-" Foreground="{StaticResource SciFiTextDim}" FontSize="12" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- RAM Info -->
                <Border Style="{StaticResource SciFiCardStyle}" Width="350" Height="150" Margin="10">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="PHYSICAL MEMORY" Foreground="{StaticResource SciFiNeonPurple}" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBlock x:Name="TxtRAM" Text="Querying WMI..." Foreground="{StaticResource SciFiTextPrimary}" FontSize="24" FontWeight="Bold"/>
                        <TextBlock Text="Total Installed Capacity" Foreground="{StaticResource SciFiTextDim}" FontSize="12" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- GPU Info -->
                <Border Style="{StaticResource SciFiCardStyle}" Width="720" Height="150" Margin="10">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="GRAPHICS PROCESSING UNIT" Foreground="{StaticResource SciFiNeonGreen}" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBlock x:Name="TxtGPU" Text="Querying WMI..." Foreground="{StaticResource SciFiTextPrimary}" FontSize="24" FontWeight="Bold"/>
                        <TextBlock x:Name="TxtGPUDriver" Text="-" Foreground="{StaticResource SciFiTextDim}" FontSize="12" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </WrapPanel>
        </StackPanel>
    </Grid>
</UserControl>
